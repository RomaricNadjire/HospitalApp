/* eslint-disable prettier/prettier */
import { View, Text } from 'react-native'
import React from 'react'
import { COLORS } from '../../outils/constantes'

const Settings = () => {
  return (
    <View>
        <Text style={{color: COLORS.black}}>Paramétres</Text>
    </View>
  );
};

export default Settings;
